[build]
  command = "npm run build"
  publish = ".next"

[functions]
  external_node_modules = ["ws"]
  node_bundler = "esbuild"

[functions."*"]
  timeout = 60

[[plugins]]
  package = "@netlify/plugin-nextjs"
